{
  "name": "@zscalerctl/engine-stdio-typescript",
  "version": "0.1.0-candidate.1",
  "private": true,
  "description": "Candidate-only zero-runtime-dependency TypeScript reference client for zscalerctl engine stdio protocol v1.",
  "type": "module",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "test": "node --test test/*.test.ts"
  }
}
